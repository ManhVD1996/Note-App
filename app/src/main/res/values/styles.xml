<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/black_transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="colorPrimaryDark">@color/black_transparent</item></style>

    <style name="CustomActionBarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/main_toolbar_color_background</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--<style name="Theme.AlertDialog" parent="@android:style/Theme.Holo.Light.Dialog">-->
        <!--<item name="android:background">@android:color/transparent</item>-->
        <!--<item name="android:textColor">#6c7bec</item>-->
        <!--<item name="android:windowTitleStyle">@style/Theme.AlertDialog.Title</item>-->
    <!--</style>-->

    <!--<style name="Theme.AlertDialog.Title" parent="@android:style/TextAppearance.DialogWindowTitle">-->
        <!--<item name="android:background">#1b0808</item>-->
    <!--</style>-->
</resources>
